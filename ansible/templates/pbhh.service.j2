[Unit]
Description=Photobooth HwHandler service
Before=photobooth.service

[Service]
WorkingDirectory=/home/{{ansible_user}}
ExecStart=/opt/photobooth/pbhh
Restart=always
RestartSec=3s

# @TODO: "is added as a dependency to a non-existent unit multi-user.target"
[Install]
WantedBy=default.target